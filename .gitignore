# Raw Wazuh dumps (huge, exceed GitHub limits) — compressed .gz versions are committed instead
AIT_ADS/*_wazuh.json

# Playbook helper scripts (not part of the dataset)
Documents/Document250playbook/PlaybookScripts/
